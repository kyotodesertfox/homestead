[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

[[redirects]]
  from   = "/api/contracts"
  to     = "/.netlify/functions/contracts"
  status = 200
  force  = true
